# 2-phase-merge-sort
When a large file is given that doesn’t fit into the main memory which has multiple columns of strings it can be sorted in 2-way Merge sort and can be extended to multi-way by certain changes. Then it is speeded up by using Threading in CPP.
